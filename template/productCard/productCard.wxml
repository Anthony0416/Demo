<template name="productCard">
  <view class="product_card">
    <navigator url="{{url}}">
      <image src="{{image}}"></image>
      <text>{{title}}</text>
      <text>¥{{price}}</text>
    </navigator>
  </view>
</template>